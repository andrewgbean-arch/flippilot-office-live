import type { DecisionPanelProps } from "./decisionPanelProps";

// Pilot's view and the Devil's Advocate ("Challenge me"). This is a placeholder:
// the analysis builder replaces this file (keep the default export and the props).
export default function AnalysisCard(_props: DecisionPanelProps) {
  return null;
}
