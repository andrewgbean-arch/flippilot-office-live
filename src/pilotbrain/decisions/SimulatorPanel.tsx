import type { DecisionPanelProps } from "./decisionPanelProps";

// The Simulator panel. This is a placeholder: the simulator builder replaces
// this file (keep the default export and the props).
export default function SimulatorPanel(_props: DecisionPanelProps) {
  return null;
}
